/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "www.acs.art.br"], // Adicione os domínios confiáveis para carregar imagens
  },
};

export default nextConfig;
