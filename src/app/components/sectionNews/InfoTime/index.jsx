function SpanInfoTime({ children }) {
  return (
    <span className="absolute font-semibold text-sm text-white right-4 top-3 uppercase">
      {children}
    </span>
  );
}

export default SpanInfoTime;
