function BoxProjects({ children }) {
  return (
    <div className="flex flex-col justify-center items-center gap-4 rounded-lg shadow-lg shadow-black/60 w-full bg-acs-gradient py-5">
      {children}
    </div>
  );
}

export default BoxProjects;
