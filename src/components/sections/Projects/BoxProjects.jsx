function BoxProjects({ children }) {
  return (
    <div className="flex flex-col justify-center items-center gap-4 rounded-lg shadow-lg shadow-blue-900/60 w-full py-5">
      {children}
    </div>
  );
}

export default BoxProjects;
